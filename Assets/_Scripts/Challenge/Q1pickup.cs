using UnityEngine;
using System.Collections;

public class Q1pickup : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
    //void OnTriggerEnter(Collider coll)
    //{
    //    if (coll.tag == "Player")
    //    {
    //        Q1Manager.S.quest_finish();
    //        Destroy(this.gameObject);
    //    }
    //}
}
